---
description: Inspect changes and create atomic commits, `/commit push` to push after
---
Review all uncommitted changes in this repo (`git status`, `git diff`, `git diff --cached`).

Group the changes into logical, atomic commits. Each commit should:
- Contain one coherent change (one idea, one fix, one feature)
- Have a clear, conventional commit message
- Be ordered so the history reads naturally

Walk me through the grouping before committing. Use `git add -p` or
file-level staging to split things up. Make the commits.

$ARGUMENTS
